name: Test
'on': push
jobs:
    anchor-test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: Anchor Test
              uses: mrgnlabs/anchor-test-action@v0.4
              with:
                  args: '--provider.cluster=localnet'
                  workspace_dir: '.'
